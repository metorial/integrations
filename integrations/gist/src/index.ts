import { Slate } from 'slates';
import { spec } from './spec';
import {
  getContact,
  listContacts,
  createOrUpdateContact,
  deleteContact,
  batchImportContacts,
  getConversation,
  createConversation,
  replyToConversation,
  searchConversations,
  manageConversation,
  getConversationCounts,
  manageTags,
  trackEvent,
  listEvents,
  listCampaigns,
  manageCampaign,
  manageArticle,
  listArticles,
  manageCollection,
  listSegments,
  manageForm,
  manageSubscriptionType,
  listTeams,
  manageStore,
  manageOrder,
  manageCart,
  manageProduct,
  manageCustomer
} from './tools';
import { contactEvents, conversationEvents, campaignEvents, meetingEvents } from './triggers';

export let provider = Slate.create({
  spec,
  tools: [
    getContact,
    listContacts,
    createOrUpdateContact,
    deleteContact,
    batchImportContacts,
    getConversation,
    createConversation,
    replyToConversation,
    searchConversations,
    manageConversation,
    getConversationCounts,
    manageTags,
    trackEvent,
    listEvents,
    listCampaigns,
    manageCampaign,
    manageArticle,
    listArticles,
    manageCollection,
    listSegments,
    manageForm,
    manageSubscriptionType,
    listTeams,
    manageStore,
    manageOrder,
    manageCart,
    manageProduct,
    manageCustomer
  ],
  triggers: [contactEvents, conversationEvents, campaignEvents, meetingEvents]
});
