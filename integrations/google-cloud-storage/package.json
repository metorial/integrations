{
  "name": "@slates-integrations/google-cloud-storage",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bunx @vercel/ncc build src/index.ts -o dist -m -s",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/node": "^20",
    "slates": "workspace:*",
    "zod": "^4.2"
  },
  "devDependencies": {
    "@slates/test": "workspace:*",
    "typescript": "^5",
    "vitest": "^3.1.2"
  },
  "version": "0.2.0-rc.1"
}