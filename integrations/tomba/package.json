{
  "name": "@slates/integration-tomba",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bunx @vercel/ncc build src/index.ts -o .ncc-dist -m -s",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/node": "^20",
    "slates": "workspace:*",
    "zod": "^4.2"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}