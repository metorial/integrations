{
  "name": "@slates/integration-pdf4me",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bunx @vercel/ncc build src/index.ts -o dist -m -s",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/node": "^20",
    "slates": "workspace:*",
    "zod": "^4.2"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}