import { Slate } from 'slates';
import { spec } from './spec';
import {
  listParties,
  getParty,
  createParty,
  updateParty,
  deleteParty,
  searchParties,
  listOpportunities,
  createOpportunity,
  updateOpportunity,
  deleteOpportunity,
  listProjects,
  createProject,
  updateProject,
  deleteProject,
  listTasks,
  createTask,
  updateTask,
  deleteTask,
  listEntries,
  createEntry,
  manageTags,
  listPipelines,
  listUsers,
} from './tools';
import {
  partyEvents,
  opportunityEvents,
  projectEvents,
  taskEvents,
  userEvents,
} from './triggers';

export let provider = Slate.create({
  spec,
  tools: [
    listParties,
    getParty,
    createParty,
    updateParty,
    deleteParty,
    searchParties,
    listOpportunities,
    createOpportunity,
    updateOpportunity,
    deleteOpportunity,
    listProjects,
    createProject,
    updateProject,
    deleteProject,
    listTasks,
    createTask,
    updateTask,
    deleteTask,
    listEntries,
    createEntry,
    manageTags,
    listPipelines,
    listUsers,
  ],
  triggers: [
    partyEvents,
    opportunityEvents,
    projectEvents,
    taskEvents,
    userEvents,
  ],
});
